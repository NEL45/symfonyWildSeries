{% extends "base.html.twig" %}

{% block body %}
    <div class="d-flex row mt-5 px-5">
         <div class="col-4 d-flex justify-content-center" >
            <img src="{{ asset('build/pulp-fiction-affiche.jpg') }}" alt="affiche pulp fiction" width="500px"/>
        </div>
        <div class="col-4 d-flex justify-content-center">
            <h1> 
                WELCOME
                TO <br>
                PULP FICTION!
            </h1>
        </div>
        <div class="col-4 d-flex justify-content-center">
            <img src="{{ asset('build/pulpfiction.png') }}" alt="poster pulp fiction" width="531px"/>
        </div>
    </div>
{% endblock %}
